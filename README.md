## Usage
Run the following command as root to install Docker and Docker Compose on Debian-based systems:
```bash
curl -fsSL https://raw.githubusercontent.com/janniknu/homelab/main/debian-docker-installation-script.sh | bash
```
